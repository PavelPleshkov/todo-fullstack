---
description: Points agents to the canonical codebase architecture map
alwaysApply: true
---

# Project context

For detailed architecture, see docs/CODEBASE_MAP.md.

- Prefer reading or updating that file instead of re-deriving the whole repo layout in chat.
- After large structural changes, update CODEBASE_MAP.md in the same PR when possible.
- Local PostgreSQL and backend env: `docs/local-database.md`, `apps/backend/.env.example`.
