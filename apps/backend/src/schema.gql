# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthPayload {
  accessToken: String!
  user: AuthUser!
}

type AuthUser {
  email: String!
  id: Int!
  role: String!
}

input CreateTaskInput {
  isDone: Boolean!
  text: String!
}

input LoginInput {
  email: String!
  password: String!
}

type MoveCompletedResult {
  moved: [Task!]!
  tasks: [Task!]!
}

type Mutation {
  createTask(input: CreateTaskInput!): Task!
  login(input: LoginInput!): AuthPayload!
  markAllActiveTasks(ownerId: Int): [Task!]!
  moveCompletedToBin(ownerId: Int): MoveCompletedResult!
  moveTaskToActive(id: Int!): Task!
  moveTaskToBin(id: Int!): Task!
  permanentlyDeleteTask(id: Int!): Boolean!
  register(input: RegisterInput!): AuthPayload!
  unmarkAllActiveTasks(ownerId: Int): [Task!]!
  updateTask(id: Int!, input: UpdateTaskInput!): Task!
}

type Query {
  activeTasks(ownerId: Int): [Task!]!
  binTasks(ownerId: Int): [Task!]!
  users: [AuthUser!]!
}

input RegisterInput {
  email: String!
  password: String!
}

type Task {
  date: String!
  id: Int!
  isDone: Boolean!
  ownerEmail: String!
  text: String!
  userId: Int!
}

input UpdateTaskInput {
  isDone: Boolean
  text: String
}