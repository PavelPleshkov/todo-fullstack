# Copy to apps/backend/.env and adjust. Do not commit .env.

PGHOST=localhost
PGPORT=5432
PGUSER=todo_user
# Empty string if your local Postgres uses trust / no password.
PGPASSWORD=
PGDATABASE=todo_db
JWT_SECRET=dev-only-change-me-to-a-long-random-string